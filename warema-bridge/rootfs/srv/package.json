{
  "scripts": {
    "start": "node index.js",
    "build": "docker buildx build --platform linux/arm/v7,linux/arm64/v8,linux/amd64 --tag santam/wms-mqtt:latest -f standalone.Dockerfile --push ../.."
  },
  "dependencies": {
    "mqtt": "^4.2.6",
    "serialport": "^10.0.0",
    "@serialport/parser-delimiter": "^10.0.0"
  }
}
